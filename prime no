#include<stdio.h>
int main()
{
    int i,n,count;
    printf("\n enter a number ");
    scanf("%d",&n);
    for(i=2;i<n/2;i++)
    {
        if(n%i==0)
        count++;
    }
    if(count==0)
        printf("\n is a prime no ");
    else
        printf("\n is not a prime no");
}